import Link from"next/link";import{requireSession}from"@/src/shared/application/session";import{backendFetch}from"@/src/shared/infrastructure/backend";import type{Simulation,Vehicle,Product}from"@/src/shared/domain/types";
export default async function Admin(){await requireSession('ADMIN');const[sims,vehicles,products,users]=await Promise.all([backendFetch<Simulation[]>('/api/simulations'),backendFetch<Vehicle[]>('/api/admin/vehicles'),backendFetch<Product[]>('/api/admin/products'),backendFetch<unknown[]>('/api/admin/users')]);return <><div className="page-title"><h1>Panel administrativo</h1><p>Control operativo de AutoCredit.</p></div><div className="mt-6 grid gap-4 md:grid-cols-4"><Metric label="Pendientes" value={sims.filter(x=>x.status==='DRAFT').length}/><Metric label="Usuarios" value={users.length}/><Metric label="Vehículos" value={vehicles.length}/><Metric label="Productos activos" value={products.filter(x=>x.active).length}/></div><div className="card mt-6 p-6"><h2 className="font-bold">Acciones prioritarias</h2><div className="mt-4 flex flex-wrap gap-3"><Link className="btn btn-primary" href="/admin/simulations">Revisar aprobaciones</Link><Link className="btn btn-secondary" href="/admin/vehicles">Gestionar vehículos</Link><Link className="btn btn-secondary" href="/admin/products">Gestionar productos</Link></div></div></>};function Metric({label,value}:{label:string,value:number}){return <div className="card metric"><small>{label}</small><strong>{value}</strong></div>}
