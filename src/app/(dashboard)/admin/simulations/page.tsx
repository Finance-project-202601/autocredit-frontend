import{requireSession}from"@/src/shared/application/session";import{backendFetch}from"@/src/shared/infrastructure/backend";import type{Simulation}from"@/src/shared/domain/types";import{money,percent,date}from"@/src/shared/presentation/format";import{ApproveButton}from"@/src/modules/admin/presentation/ApproveButton";
export default async function Approvals(){await requireSession('ADMIN');const rows=await backendFetch<Simulation[]>('/api/simulations');return <><div className="page-title"><h1>Aprobaciones</h1><p>Revisa y formaliza simulaciones de clientes.</p></div><div className="card table-wrap mt-6"><table className="data-table"><thead><tr><th>Fecha</th><th>Financiado</th><th>Cuota</th><th>Balón</th><th>TCEA</th><th>Estado</th><th></th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{date(x.createdAt)}</td><td>{money.format(x.financedAmount)}</td><td>{money.format(x.basePayment)}</td><td>{money.format(x.balloonAmount)}</td><td>{percent(x.tcea)}</td><td><span className="badge">{x.status}</span></td><td><ApproveButton id={x.id} disabled={x.status!=='DRAFT'}/></td></tr>)}</tbody></table></div></>}
