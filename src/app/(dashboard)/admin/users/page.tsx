import{requireSession}from"@/src/shared/application/session";import{backendFetch}from"@/src/shared/infrastructure/backend";import{date}from"@/src/shared/presentation/format";type User={id:string;email:string;role:string;enabled:boolean;createdAt:string};export default async function Users(){await requireSession('ADMIN');const rows=await backendFetch<User[]>('/api/admin/users');return <><div className="page-title"><h1>Usuarios</h1><p>Cuentas registradas en AutoCredit.</p></div><div className="card table-wrap mt-6"><table className="data-table"><thead><tr><th>Correo</th><th>Rol</th><th>Registro</th><th>Estado</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.email}</td><td>{x.role}</td><td>{date(x.createdAt)}</td><td><span className="badge">{x.enabled?'Activo':'Bloqueado'}</span></td></tr>)}</tbody></table></div></>}
