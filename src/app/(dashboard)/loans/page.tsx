import Link from"next/link";import{backendFetch}from"@/src/shared/infrastructure/backend";import type{Loan}from"@/src/shared/domain/types";import{date}from"@/src/shared/presentation/format";
export default async function Loans(){const rows=await backendFetch<Loan[]>('/api/loans');return <><div className="page-title"><h1>Mis préstamos</h1><p>Consulta condiciones aprobadas y descarga tu plan de pagos.</p></div><div className="card table-wrap mt-6"><table className="data-table"><thead><tr><th>Préstamo</th><th>Aprobación</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.id.slice(0,8).toUpperCase()}</td><td>{date(x.approvedAt)}</td><td><span className="badge">{x.status}</span></td><td className="flex gap-4"><Link className="font-semibold text-blue-700" href={`/loans/${x.id}`}>Cronograma</Link><a className="font-semibold text-blue-700" href={`/api/backend/loans/${x.id}/report`}>PDF</a></td></tr>)}</tbody></table></div></>}
